import styled from 'styled-components';
import BasicCell from './BasicCell';

const WallCell = styled(BasicCell)`
  outline: solid 1px black;
  background-color: darkgray;
`;

export default WallCell;
